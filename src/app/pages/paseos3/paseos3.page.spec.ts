import { ComponentFixture, TestBed } from '@angular/core/testing';
import { Paseos3Page } from './paseos3.page';

describe('Paseos3Page', () => {
  let component: Paseos3Page;
  let fixture: ComponentFixture<Paseos3Page>;

  beforeEach(() => {
    fixture = TestBed.createComponent(Paseos3Page);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
